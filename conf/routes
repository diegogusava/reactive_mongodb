# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                    controllers.Application.index

# Users
GET         /users               controllers.Application.list
GET         /users/:login        controllers.Application.find(login)
POST        /users               controllers.Application.insert
PUT         /users               controllers.Application.update
DELETE      /users/:login        controllers.Application.delete(login)

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file        controllers.Assets.at(path="/public", file)
